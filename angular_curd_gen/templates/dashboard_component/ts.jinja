{% set component_name = app_title_name+'DashboardComponent' -%}

import {Component, OnInit} from '@angular/core';

@Component({
  selector: 'app-{{ lower_app_name }}-dashboard',
  templateUrl: './{{ lower_app_name }}-dashboard.component.html',
  styleUrls: ['./{{ lower_app_name }}-dashboard.component.css'],
  standalone: true,
})
export class {{ component_name }} implements OnInit {
  models = ['{{ lower_models_name }}']
  modelsTranslations = new Map([['{{ lower_models_name }}', '{{ model_readable_name }}']]);


  constructor(
  ) {
  }

  ngOnInit(): void {
  }

}
