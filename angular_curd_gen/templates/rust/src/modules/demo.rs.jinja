use lazy_static::lazy_static;
use crate::config::SETTINGS;
use crate::utils;

lazy_static! {
    pub static ref MODULE_SETTINGS: utils::settings::ModuleSettings = {
        utils::settings::ModuleSettings {
            name: "demo".to_string(),
        }
    };

    pub static ref MODULE_LOGGER: utils::log::MongoLogger = {
        utils::log::MongoLogger::new(SETTINGS.project.as_str(),MODULE_SETTINGS.name.as_str())
    };
}