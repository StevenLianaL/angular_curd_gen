import { TestBed } from '@angular/core/testing';

import { {{ model_name }}Service } from './{{ lower_model_name }}_api.service';

describe('{{ model_name }}Service', () => {
  let service: {{ model_name }}Service;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject({{ model_name }}Service);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
